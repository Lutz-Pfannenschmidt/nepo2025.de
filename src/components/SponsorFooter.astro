---
const sponsors = [
    ["Noch keine Sponsor*innen 😨 - Ihr Name könnte hier stehen!", "#"],
];

const html = sponsors
    .map(([name, url]) => `<a href="${url}">${name}</a>`)
    .join(" | ");
---

<footer class="footer footer-center bg-base-300 text-base-content p-4">
    <aside>
        <p set:html={html} />
    </aside>
</footer>
